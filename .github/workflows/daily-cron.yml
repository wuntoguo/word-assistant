# 每日爬取 + 推荐预计算（解决 Fly.io scale-to-zero 时 cron 不运行）
# 需在 Repo Settings → Secrets 添加 CRON_SECRET
name: Daily Crawl

on:
  schedule:
    # 美西早上 07:00（自动覆盖冬令时/夏令时）
    # - PST(UTC-8): 15:00 UTC
    # - PDT(UTC-7): 14:00 UTC
    - cron: '0 14 * * *'
    - cron: '0 15 * * *'
  workflow_dispatch:      # 支持手动运行

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger daily pipeline (async - 立即返回，后台执行约 5–10 分钟)
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          # 只在美西时间 07:00 触发，避免双 cron 在非目标小时执行
          LA_HOUR=$(TZ=America/Los_Angeles date +%H)
          if [ "$LA_HOUR" != "07" ]; then
            echo "Skip: current America/Los_Angeles hour is $LA_HOUR, target is 07."
            exit 0
          fi

          # 去掉可能的换行（粘贴时带入）
          CRON_SECRET=$(printf '%s' "$CRON_SECRET" | tr -d '\n\r')
          if [ -z "$CRON_SECRET" ]; then
            echo "::error::CRON_SECRET not set. Add it in Repo Settings → Secrets (must match Fly.io)."
            exit 1
          fi
          resp=$(curl -s -w "\n%{http_code}" -X POST "https://feedlingo.fly.dev/api/cron/daily?async=1" \
            -H "Authorization: Bearer $CRON_SECRET" \
            -H "Content-Type: application/json") || true
          code=$(echo "$resp" | tail -n1)
          body=$(echo "$resp" | sed '$d')
          echo "HTTP $code"
          echo "Body: $body"
          if [ "$code" != "202" ]; then
            echo "::error::Expected 202, got $code. Check CRON_SECRET in repo secrets."
            exit 1
          fi
